{
  "name": "duospace-signaling",
  "version": "0.1.0",
  "private": true,
  "description": "DuoSpace Call Gateway — authenticated WebSocket signaling server (Phase 1 scaffold, migration brief STEP 6). Stateless where possible; validates every message's sender/recipient/call id/timestamp/event type and never trusts a client-claimed sender id.",
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "vitest run"
  },
  "dependencies": {
    "ws": "^8.18.0",
    "jose": "^5.9.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/ws": "^8.5.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
