<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Needed for startBluetoothSco()/stopBluetoothSco() route switching to
         a connected Bluetooth headset. Merged into the host app's manifest
         automatically by Gradle manifest merging on `cap sync`. -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
</manifest>
