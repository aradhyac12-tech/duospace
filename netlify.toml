[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA fallback — client-side routes (/auth, /chat, ...) must serve index.html.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
